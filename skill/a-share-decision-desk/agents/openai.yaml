interface:
  display_name: "A-Share Decision Desk"
  short_description: "A-share pre-open decision workflow"
  brand_color: "#C1272D"
  default_prompt: "Use $a-share-decision-desk to build tomorrow's A-share game plan from overnight events, policy timing, and opening watchlists."

policy:
  allow_implicit_invocation: true
